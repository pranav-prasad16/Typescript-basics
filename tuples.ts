const tUser: [string, number, boolean] = ['hello', 25, true]; // can only store data in the order written

let rgb: [number, number, number];

rgb = [243, 32, 29]; // can't have more than 3 numbers as there are only 3 no.s in the tupple declaration
